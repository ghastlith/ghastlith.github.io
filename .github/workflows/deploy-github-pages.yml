name: Deploy GitHub Pages
run-name: Deploy Website to GitHub Pages
on: [ push ]
jobs:
  build:
    runs-on: ubuntu-latest
    name: Running Node Command
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm run deploy
