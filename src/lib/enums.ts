export enum Page {
  HOME = "home",
  WONDERLAND = "wonderland",
  NOT_FOUND = "not found",
}

export enum External {
  GITHUB = "github",
  TRYHACKME = "tryhackme",
}

export enum Target {
  BLANK = "_blank",
  SELF = "_self",
}
