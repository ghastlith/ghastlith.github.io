{
  "name": "blarknes.github.io",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm i && npm run build && gh-pages -d build -t true"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^2.0.3",
    "@sveltejs/kit": "^1.20.4",
    "sass": "^1.69.0",
    "svelte": "^4.0.5",
    "svelte-preprocess": "^5.0.4",
    "vite": "^4.4.2"
  },
  "type": "module",
  "dependencies": {
    "gh-pages": "^6.0.0",
    "svelte-icons": "^2.1.0"
  }
}
